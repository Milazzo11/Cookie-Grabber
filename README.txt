This program extracts data from a specified cookie or cookies and sends it to a Discord webhook.

The cookies are extracted from both Chrome and Firefox browsers, and are filtered by name.
Currently, the program extracts data from a Roblox.com cookie that allows users to log in, but it can be changed.

Enjoy :)